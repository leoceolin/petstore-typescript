/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { usersCreateWithList } from "../../funcs/usersCreateWithList.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: z.array(components.User$inboundSchema).optional(),
};

export const tool$usersCreateWithList: ToolDefinition<typeof args> = {
  name: "users-create-with-list",
  description: `Creates list of users with given input array

Creates list of users with given input array`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersCreateWithList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
